name: flash_chat
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  animated_text_kit: ^4.2.2
  firebase_core: ^2.27.1
  firebase_auth: ^4.17.9
  cloud_firestore: ^4.15.9
  modal_progress_hud_nsn: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: "^0.13.1" # Add the package here

flutter:
  uses-material-design: true
  assets:
    - images/

# flutter_icons configuration block starts here (at the root level)
flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "images/logo1.png" # Path to your icon image
  min_sdk_android: 21